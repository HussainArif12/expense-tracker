[project]
name = "expense-tracker-backend"
version = "0.1.0"
description = "Backend for expense tracker"
requires-python = ">=3.12"
dependencies = [
    "fastapi",
    "python-multipart"
]

[dependency-groups]
dev = [
  "workers-py",
  "workers-runtime-sdk"
]